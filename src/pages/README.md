# pages/

路由页面组件目录。

## 文件

- `HomePage.tsx` - 首页仪表盘：显示 PTT 提示、历史统计汇总与区间趋势图。
- `SettingsPage.tsx` - 设置页：应用偏好、大模型（GLM / 千问二选一、各自密钥与区域）、存储（ERPNextCN 归档）、快捷键、关于；保存写入 `app` / `asr`（含 `provider`、`qwenApiKey`、`qwenRegion`）/ `erpnextcnDty`。
- `HistoryPage.tsx` - 历史记录页：搜索/排序/分组展示，支持复制、删除与清空。
